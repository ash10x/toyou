# Server utilities

This folder contains the database client and query helpers for Neon/Postgres.

Environment variable required:

- `NEON_DATABASE_URL` - your Neon Postgres connection string

Usage: import the helpers from `server/db/queries` in server-side code or API routes.
